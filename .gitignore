# Personal Claude Code context (not for the public repo)
CLAUDE.local.md

# Python
__pycache__/
*.py[cod]
*.egg-info/
.venv/
venv/
env/

# Local artifacts (regenerable via corpus_pretrain_g1_probe --save)
data/checkpoints/

# Editors
.vscode/
.idea/
*.swp
.DS_Store

# Bonded-adversarial raw per-step traces (~1.6MB each, regenerable);
# keep only the distilled digest-*.json in the ledger.
docs/findings/logs/bonded_adversarial/run-*.json
