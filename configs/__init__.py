# configs/ contains YAML and JSON configuration files, not Python modules.
# Load them directly with PyYAML or json:
#
#   import yaml
#   with open("configs/field.yaml") as f:
#       field_config = yaml.safe_load(f)
