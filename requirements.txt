# RFE-Core2 requirements
# Core
numpy>=1.24
torch>=2.0
networkx>=3.0

# API
fastapi>=0.100
uvicorn>=0.22
websockets>=11.0
pydantic>=2.0

# Visualization (optional but recommended)
matplotlib>=3.7

# Config parsing (optional — configs/*.yaml degrade gracefully without it)
pyyaml>=6.0

# Removed as unused (audited 2026-07-02): scipy (no import anywhere — the FFT
# path is numpy), pytest (the test suite is plain runnable scripts by design —
# tests/README.md; wrap in pytest yourself if you want assertions).
# Optional LLM encoder backend deps (transformers / accelerate / bitsandbytes /
# llama-cpp-python) are deliberately NOT listed — see
# docs/local_model_integration/IMPLEMENTATION_GUIDE.md §1.
